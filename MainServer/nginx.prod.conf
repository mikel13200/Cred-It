upstream credit_system {
    server web:8000;
}

upstream frontend_react {
    server frontend:80;
}

server {
    listen 80;
    server_name 217.216.35.29 localhost 127.0.0.1;
    
    # Client body size
    client_max_body_size 20M;
    client_body_buffer_size 128k;

    # Timeouts
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    send_timeout 60s;

    # Logging
    access_log /var/log/nginx/credit_system_access.log;
    error_log /var/log/nginx/credit_system_error.log;

    # --- FRONTEND (Root) ---
    location / {
        proxy_pass http://frontend_react;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # --- API (Django) ---
    location /api/ {
        proxy_pass http://credit_system/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # --- ADMIN (Django) ---
    location /admin/ {
        proxy_pass http://credit_system/admin/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # --- REACT STATIC FILES ---
    location /static/js/ {
        proxy_pass http://frontend_react;
    }
    location /static/css/ {
        proxy_pass http://frontend_react;
    }
    location /static/media/ {
        proxy_pass http://frontend_react;
    }

    # --- DJANGO STATIC/MEDIA ---
    location /static/ {
        alias /var/www/credit_system/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    location /media/ {
        alias /var/www/credit_system/media/;
        expires 7d;
        add_header Cache-Control "public";
    }

    # Health check endpoint
    location /health/ {
        proxy_pass http://credit_system;
        proxy_set_header Host $host;
        access_log off;
    }
}
